% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vrt.R
\name{make_vrt}
\alias{make_vrt}
\title{Generate VRT wrappers for GeoTIFF release assets}
\usage{
make_vrt(repo = get_default_repo(), tag = get_default_tag())
}
\arguments{
\item{repo}{Repository slug in the form \code{"owner/name"}. Uses the package
default if not supplied.}

\item{tag}{Release tag. Uses the package default if not supplied.}
}
\value{
A character vector with the paths of the VRT files that were created.
}
\description{
Creates GDAL VRT files for each Cloud-Optimized GeoTIFF stored as a GitHub
release asset. The function resolves the signed asset URL to emit a
VRT that references the remote file via \verb{/vsicurl/}.
}
\examples{
\dontrun{
make_vrt(repo = "rolfsimoes/cogs", tag = "cog-test")
}

}
