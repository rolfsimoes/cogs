% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legend.R
\name{rg_legend}
\alias{rg_legend}
\title{Write Legend / Color Table}
\usage{
rg_legend(file, values, colors_rgba, labels = NULL)
}
\arguments{
\item{file}{Character string specifying the target file path (GTiff or VRT)}

\item{values}{Integer vector of pixel values to assign colors}

\item{colors_rgba}{Matrix or data frame with 4 columns (R, G, B, A) specifying RGBA colors (0-255)}

\item{labels}{Character vector of category labels corresponding to values (optional, default: NULL)}
}
\value{
NULL (invisibly). The function modifies the file in place.
}
\description{
Attach a color table and category labels to a GeoTIFF or VRT file.
This function uses GDAL's color table functionality to add visualization metadata.
}
\examples{
\dontrun{
# Create a simple color table
values <- c(1, 2, 3, 4)
colors <- matrix(c(
  255, 0, 0, 255,    # Red
  0, 255, 0, 255,    # Green
  0, 0, 255, 255,    # Blue
  255, 255, 0, 255   # Yellow
), ncol = 4, byrow = TRUE)
labels <- c("Forest", "Water", "Urban", "Agriculture")

rg_legend("landcover.tif", values, colors, labels)

# Without labels
rg_legend("landcover.tif", values, colors)
}

}
