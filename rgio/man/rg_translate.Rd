% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translate.R
\name{rg_translate}
\alias{rg_translate}
\title{Translate Raster Datasets}
\usage{
rg_translate(
  src,
  dst,
  format = NULL,
  co = NULL,
  resample = NULL,
  nodata = NA_real_,
  options = character(),
  threads = 0L
)
}
\arguments{
\item{src}{Source raster file path.}

\item{dst}{Destination raster file path.}

\item{format}{Optional GDAL driver name for the output dataset (default: `NULL`, meaning reuse source).}

\item{co}{Character vector of GDAL creation options.}

\item{resample}{Optional resampling method to apply when resizing (same aliases as [`rg_read()`]).}

\item{nodata}{Optional numeric value to set as nodata in the output; use `NA` to skip.}

\item{options}{Additional raw GDAL translate arguments supplied as character vector (e.g. `c("-projwin", ...)`).}

\item{threads}{Number of threads to request from GDAL (default: 0 for auto).}
}
\value{
Invisibly returns `dst`.
}
\description{
Lightweight wrapper around GDAL's `gdal_translate` utility for converting datasets,
changing formats, or applying simple pixel-space operations.
}
